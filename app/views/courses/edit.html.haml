- content_for :title do
  EiuELearning
  \| 
  Editing
  = @course.title
  
%br  
.container    
  .card
    .card-header
      %h3
        .fa.fa-graduation-cap
        Editing course
    .card-body
      = render 'form'
  
    .card-footer
      = link_to 'Show', @course
      \|
      = link_to 'Back', courses_path
